package kdr.gui.popup;

import kdr.gui.*;

public class KCirclePopup extends KFigurePopup {
    public KCirclePopup(DrawerView view, String title, boolean fillFlag) {
        super(view, title, fillFlag);
    }
}
